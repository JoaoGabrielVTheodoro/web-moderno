/**
 * 25) Escrever um programa para exibir os números de 1 até 50 na tela.
*/

const exibirNumero = (inicio = 1, fim = 50) => {
  for (let i = inicio; i <= fim; i++) {
    console.log(i)
  }
}

exibirNumero()
